-r requirements.base.txt

# Add new project dependencies below this line.
# Docker installs requirements.base.txt in a stable cached layer first, then
# processes this file so new dependencies do not force reinstalling the base set.
feedparser>=6.0.0
regex>=2024.0.0
xgboost>=2.0.0
optuna>=3.6.0
capstone>=5.0.0
OTXv2>=1.5.12
